/**
 * Repository package — Spring Data JPA repositories.
 *
 * Current repositories:
 * - UserRepository    — CRUD + email lookup for User entity
 *
 * Future additions:
 * - ResumeRepository         — Find resumes by user, status, upload date
 * - JobPostingRepository     — Find active jobs, search by keyword/skills
 * - ScreeningRepository      — Find screenings by job/applicant/status
 * - ScreeningResultRepository — Ranked results for a given screening session
 */
package com.resumescreening.repository;
