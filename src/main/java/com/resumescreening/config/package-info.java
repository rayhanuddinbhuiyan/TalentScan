/**
 * Configuration package.
 *
 * Future additions:
 * - SwaggerConfig (OpenAPI/Swagger UI)
 * - AppConfig (application-level beans)
 * - AsyncConfig (thread pool executors for AI processing)
 * - CacheConfig (Redis caching for screening results)
 */
package com.resumescreening.config;
