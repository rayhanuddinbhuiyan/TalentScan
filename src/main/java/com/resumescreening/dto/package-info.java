/**
 * DTO (Data Transfer Object) package.
 *
 * Current DTOs:
 * - RegisterRequest   — User registration payload
 * - LoginRequest      — Login credentials payload
 * - AuthResponse      — JWT + user info after auth
 * - UserResponse      — User profile response
 *
 * Future additions:
 * - ResumeUploadRequest   — Resume file + metadata
 * - ResumeResponse        — Parsed resume data
 * - JobPostingRequest     — Job posting creation payload
 * - JobPostingResponse    — Job posting details
 * - ScreeningResultResponse — AI screening score + ranked candidates
 * - PagedResponse<T>      — Generic paginated response wrapper
 */
package com.resumescreening.dto;
