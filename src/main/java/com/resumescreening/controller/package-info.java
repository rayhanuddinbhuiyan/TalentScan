/**
 * Controller package.
 *
 * Current controllers:
 * - AuthController (/api/auth)
 *
 * Future additions:
 * - ResumeController    (/api/resumes)   — Upload, parse, list resumes
 * - JobController       (/api/jobs)      — Create and manage job postings
 * - ScreeningController (/api/screening) — Trigger AI screening, view results
 * - UserController      (/api/users)     — Admin user management
 */
package com.resumescreening.controller;
