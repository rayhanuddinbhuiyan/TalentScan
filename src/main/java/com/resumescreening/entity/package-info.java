/**
 * Entity package — JPA entities mapped to database tables.
 *
 * Current entities:
 * - User       — Registered system users (APPLICANT, RECRUITER, ADMIN)
 *
 * Future additions:
 * - Resume        — Uploaded resume files + parsed text
 * - JobPosting    — Job descriptions posted by recruiters
 * - Screening     — AI screening session linking a resume to a job
 * - ScreeningResult — Individual match scores and AI explanations
 */
package com.resumescreening.entity;
